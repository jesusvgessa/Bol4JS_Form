var nombre = window.document.getElementById('nombre').addEventListener('onblur', obNombre, true);

function obNombre() {
    var nombre = document.getElementById("nombre");
    x.value = x.value.toUpperCase();
}